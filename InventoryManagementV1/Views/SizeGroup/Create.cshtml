@model InventoryManagementV1.Models.SizeGroup
@{
    Layout = "~/Views/Shared/layout.cshtml";
}

<!DOCTYPE html>
<section class="content-header"></section>
<section class="content">
    <div class="row">
        <!-- left column -->
        <div class="col-md-8">
            <!-- general form elements -->
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Size Group Create</h3>
                </div>
                <!-- /.box-header -->

                @using (Html.BeginForm("Create", "SizeGroup", FormMethod.Post, new { @class = "form-horizontal" }))
                {
                    <div class="box-body">

                        <div class="form-group">
                            <label class="control-label col-md-3">Color Name<span style="color:red;">*</span></label>
                            <div class="col-md-6">
                                @Html.TextBoxFor(m => m.Size_Group_Range, new { @Class = "form-control", @placeholder = "Size Group Range" })
                            </div>
                        </div>



                        <div class="form-group">
                            <div class="col-md-3"></div>
                            <div class="col-md-5">
                                <input type="submit" value="Save" class="btn btn-info" />
                                <input type="submit" value="Reset" id="reset" class="btn btn-info" />
                            </div>

                        </div>
                    </div>
                }


            </div>

            <!-- /.box -->

        </div>
        <!--/.col (left) -->
    </div>
    <!-- /.row -->
</section>
@section scripts{

    <script>
        $(document).ready(function () {
            $("form").submit(function (event) {
                if ($("#Size_Group_Range").val() == "") {
                    event.preventDefault();
                    swal("Failed", "Size Group Range is required", "error").then(function () {
                        $("#Size_Group_Range").focus();
                    });
                }

                else {
                    event.preventDefault();
                    $.ajax({
                        url: "/SizeGroup/Create",
                        type: "POST",
                        data:
                       {
                           Size_Group_Range: $("#Size_Group_Range").val()
                       },
                        success: function (data) {
                            if (data == "true")
                                swal("Success", "Successfully Saved", "success");
                            else
                                swal("Failed", data, "error");
                            resetfunction();
                        },
                        error: function (data) {
                            alert(data)
                        },

                    })
                }

            });
            function resetfunction() {
                $("#Size_Group_Range").val("")
            }
            $("#reset").click(function () {
                resetFunction();
            });
        });
    </script>
}